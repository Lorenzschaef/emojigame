{
  "name": "emojigame",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-client": "cd client && elm make src/Main.elm --output=public/dist.js && lessc style.less public/style.css && cd ..",
    "build-server": "tsc --build server/tsconfig.json",
    "build": "npm run build-client && npm run build-server",
    "run-server": "node server/dist/server/server.js",
    "start": "npm run build && npm run run-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "less": "^3.11.1"
  }
}
